<h2>What ship would you like to board?</h2>

<ul>
  <% @ships.each do |ship| %>
    <li>
      <%= link_to ship.name, user_ships_path %>
      0/<%= ship.capacity %>
    </li>
  <% end %>
</ul>

<%= link_to 'Create a new ship', new_user_ship_path %>